# 廁所互動小網頁 — 需求規格書

> 本文件為專案的「唯一需求依據」。任何製作或更動前，先對照本文件；有變更先更新此文件再動程式碼。
> 最後更新：2026-07-28

---

## 1. 專案簡介

一個桌機用的互動小網頁：一個**跟隨滑鼠游標的英文字**會隨著游標所在區域切換，透過「移動 → 提示 → 點擊 / 長按」一步步引導體驗者走完一段「廁所體驗」流程（尿急 → 找廁所 → 尿尿 → 沖水 → 洗手 → 解脫）。

**情緒主軸：** 全程游標常駐字為 `URGENCY`（尿急），完成沖水流程後轉為 `RELIEF`（解脫）。

---

## 2. 網站規格

| 項目 | 設定 |
|------|------|
| 畫布尺寸 | **1920 × 1080 px**（16:9 橫向，桌機全螢幕） |
| 縮放方式 | 等比縮放貼合視窗，不變形（不同螢幕自動適應） |
| 目標裝置 | 桌機 / 筆電（滑鼠游標體驗，非觸控） |
| 技術 | 單一 HTML 檔 + 原生 CSS + 原生 JavaScript（無框架，好維護、省 token） |
| 素材 | 場景使用「使用者提供的滿版圖」；互動層（文字、堆積字、水流、轉場）由程式繪製 |
| **視覺風格** | **Dot Art 黑白點點藝術風**（見 2.1） |

### 2.1 視覺風格：Dot Art 黑白點點

- 全站 5 張場景圖皆為**黑白點點藝術風**（由 Dot Art 工具將照片轉換而成）。
- **輸出格式：PNG**（點點邊緣銳利，JPEG 會產生糊邊雜訊）。
- **5 張圖需使用相同的 Dot Art 參數**（點密度、對比、Sharpen）輸出，確保風格一致。
- 互動層（游標文字、`(PEE)`/`(WATER)` 堆積、水流、可點高亮、轉場）皆配合黑白點點調性設計：以黑 / 白為主色，游標文字用等寬字，可點高亮改用「反白 / 點狀外框」而非彩色發光。

---

## 3. 核心機制

### 3.1 跟隨游標的文字
- 隱藏系統原生游標，改由一段文字跟隨滑鼠移動。
- 平常顯示「常駐字」；滑鼠移到可互動區域時，切換成該區的「提示字」。
- **文字格式：所有顯示文字前後一律加上 `()`**，例如 `(URGENCY)`、`(OPEN)`、`(PEE)`、`(WATER)`、`(RELIEF)`。（本文件表格內為求精簡以純字標示，實際畫面一律含括號。）
- **字體：DotGothic16**（點陣風無襯線，配 Dot Art）。描邊 0.75px，大小約 18px（可調）。
- **字色依場景自動切換**：每個場景擇一 —— 「黑字+白描邊」或「白字+黑描邊」，取背景最清楚者。（開發時按 `C` 可手動切換比較，決定後固定為該場景預設。門 = 黑字+白描邊。）

### 3.2 常駐字狀態機（全站同步）
| 狀態 | 常駐字 | 觸發條件 |
|------|--------|----------|
| 初始 | `URGENCY` | 進站即為此狀態 |
| 完成 | `RELIEF` | **沖水（FLUSH）動作完成後**永久切換 |

> 未完成沖水前，全站常駐字都維持 `URGENCY`；完成後全站（含全景、洗手台）都變 `RELIEF`。

---

## 4. 場景地圖與流程

共 **5 張圖**、**4 個場景**（B 全景為中樞 HUB）。

```
🚪 A. 門（1張）
   常駐 URGENCY｜滑過門把 → OPEN｜點擊 → 漸暗轉場
        ↓
🛁 B. 廁所全景 HUB（1張，含馬桶+洗手台）  ← 中樞
   常駐 URGENCY
   ├ 滑到馬桶區   → TOILET → 點擊 → 進 C
   └ 滑到洗手台區 → SINK   → 點擊 → 進 D
        ↓                          ↓
🚽 C. 馬桶（2張）             🚰 D. 洗手台（1張）
        ↑ ← BACK                    ↑ ← BACK
```

---

## 5. 各場景詳細規格

### A. 門 `door`（1 張圖）
| 區域 | 觸發 | 提示字 | 動作 |
|------|------|--------|------|
| 門把 | hover | `OPEN` | — |
| 門把 | 點擊 | — | 畫面漸暗（fade to black）→ 淡入全景 HUB |
- 常駐字：`URGENCY`

### B. 廁所全景 HUB `hub`（1 張圖，含馬桶+洗手台）
| 區域 | 觸發 | 提示字 | 動作 |
|------|------|--------|------|
| 馬桶區 | hover | `TOILET` | 該區微微高亮 |
| 馬桶區 | 點擊 | — | 漸暗 → 進馬桶場景 C |
| 洗手台區 | hover | `SINK` | 該區微微高亮 |
| 洗手台區 | 點擊 | — | 漸暗 → 進洗手台場景 D |
- 常駐字：`URGENCY`（未沖水前）/ `RELIEF`（沖水後）

### C. 馬桶 `toilet`（2 張圖：蓋關 / 蓋開）
| 區域 | 觸發 | 提示字 | 動作 |
|------|------|--------|------|
| 馬桶蓋 | hover | `LIFT` | — |
| 馬桶蓋 | 點擊 | — | 切換為「掀蓋」圖；常駐字改為 `PEE` |
| 馬桶內部 | 長按 | `PEE` | 持續產生 `(PEE)` 文字堆積在馬桶內 |
| 沖水把手 | hover | `FLUSH` | — |
| 沖水把手 | 點擊 | — | 堆積字**旋轉沖走消失**；沖水完成 → 常駐字永久變 `RELIEF` |
| 左上角 | hover / 點擊 | `← BACK` | 返回全景 HUB |
- 進入時：蓋關閉圖，常駐字沿用 `URGENCY`

### D. 洗手台 `sink`（1 張圖）
| 區域 | 觸發 | 提示字 | 動作 |
|------|------|--------|------|
| 水龍頭 | hover | `OPEN` / `CLOSE` | 依目前狀態顯示對應字 |
| 水龍頭 | 點擊 OPEN | — | `WATER` 粒子從**水龍頭出水口持續冒出**，受重力往下流進洗手台並堆積；停留後漸縮消失（模擬流入下水道） |
| 水龍頭 | 點擊 CLOSE | — | 停止出水 |
| 水流 | 滑鼠撥動 | `WATER` | **撥水物理**：滑鼠靠近「正在流下的水流」時把水撥開 / 改變流向（模擬把手伸到水龍頭下洗手）。被撥出洗手台範圍的粒子，**超出後 0.5 秒內直接消失**，保持畫面乾淨 |
| 左上角 | hover / 點擊 | `← BACK` | 返回全景 HUB |
- 常駐字：`URGENCY` / `RELIEF`（依狀態機）

---

## 6. 場景切換方法（體驗保證）

1. **統一轉場**：所有場景切換用「漸暗 → 淡入」(fade to black，約 0.6 秒)，全站一致。
2. **可點區域高亮**：滑鼠移到可互動區時，該區出現微微發光邊框，提示「這裡能點」。
3. **子場景固定返回**：馬桶 / 洗手台左上角固定 `← BACK` 熱區，隨時回全景，不卡關。
4. **常駐字全站同步**：沖水完成後所有場景常駐字一致變 `RELIEF`。

---

## 7. 動態效果（由程式製作，使用者不需畫）

| 效果 | 說明 |
|------|------|
| `(PEE)` 堆積 | 馬桶內長按時，`(PEE)` 文字持續生成、往下堆疊（參考使用者 AI 圖效果） |
| 沖水旋轉 | 點 FLUSH 後堆積字整體旋轉、縮小、淡出，像被水沖走 |
| `WATER` 水龍頭出水 | `WATER` 粒子由水龍頭出水口生成，受重力往下流進台面堆積，停留後漸縮消失（模擬排水） |
| `WATER` 撥水物理 | 滑鼠靠近「正在流下的水流」時撥開 / 改變其流向（模擬手伸到水龍頭下）；每個粒子有速度、受重力與滑鼠施力。被撥出洗手台範圍者，超出後 0.5 秒內消失。**粒子數量設上限（約 60~80）自動回收，維持流暢** |
| 場景漸暗轉場 | 黑幕淡入淡出 |
| 可點區高亮 | 發光邊框 / 輕微放大 |

---

## 8. 需要使用者提供的素材清單

全部 **1920 × 1080 px**、**PNG**、**Dot Art 黑白點點風（相同參數輸出）**、滿版。建議檔名（英文，利於 GitHub / Netlify）：

| # | 檔名建議 | 內容 | 狀態 |
|---|----------|------|------|
| 1 | `door.png` | 門（關著，正面），含可辨識的門把 | ⬜ 待提供 |
| 2 | `hub.png` | 廁所全景，畫面含馬桶 + 洗手台 | ⬜ 待提供 |
| 3 | `toilet-closed.png` | 馬桶（蓋子關閉） | ⬜ 待提供 |
| 4 | `toilet-open.png` | 馬桶（蓋子掀開，露出內部） | ⬜ 待提供 |
| 5 | `sink.png` | 洗手台（含水龍頭） | ⬜ 待提供 |

> 提供圖後，我會在各圖上「框選互動熱區座標」，再實作提示字與動作。

---

## 9. 技術與部署

| 項目 | 規劃 |
|------|------|
| 檔案結構 | `index.html`（主檔）+ `images/`（場景圖）；必要時拆 `style.css` / `script.js` |
| 版本控制 | 推上 GitHub（利於版本更新與回溯） |
| 上線 | 部署到 Netlify，產生公開連結 |
| Token 成本 | 低。場景用使用者圖片（不佔對話 token），互動層為手寫程式碼 |

---

## 10. 開放 / 待確認項目

- [ ] 5 張場景圖尚未提供（見第 8 節）
- [ ] 各圖的互動熱區座標，需拿到圖後標定
- [x] 游標字體：**DotGothic16**（點陣風，Google Fonts 載入）
- [x] 游標文字樣式：**黑字 + 白描邊 0.75px**（取代原 mix-blend-mode）
- [ ] `(URGENCY)` 緊急感效果（例：輕微抖動 / 脈動），於後續流程中加入
- [ ] 手機 / 觸控裝置的降級體驗（先以桌機為主，之後再議）
- [ ] 全站背景音 / 音效（目前未納入，如需要再加）

---

## 11. 版本記錄

| 日期 | 變更 |
|------|------|
| 2026-07-28 | 建立初版規格：確認 5 圖 / 4 場景、HUB 中樞結構、URGENCY→RELIEF 狀態機、各區提示字與動作、漸暗轉場與 ← BACK 返回 |
| 2026-07-28 | 洗手台新增「撥水物理」：滑鼠撥動影響 WATER 粒子流向，噴濺出範圍者 0.5 秒內消失；粒子數設上限維持流暢 |
| 2026-07-28 | 修正撥水邏輯：WATER 由水龍頭出水口生成並受重力下流，滑鼠撥動的是「正在流下的水流」（模擬手伸到水龍頭下），非憑空水面 |
| 2026-07-28 | 文字格式規則：所有顯示文字前後一律加上括號，例如 (OPEN)、(PEE) |
| 2026-07-28 | 確立視覺風格為 Dot Art 黑白點點藝術風；5 張圖用相同參數輸出為 PNG；互動層配合黑白調性 |
